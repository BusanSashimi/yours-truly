# Claude session operations log — 2026-06-13

_Local Mac session (began 2026-06-12 evening, rolled past midnight). Times
approximate, KST. Theme: selectable design templates — a 4-template system
the couple picks at create time and can switch in the editor, applied to the
public guest page. No commits/pushes/deploys this session; all changes are
local working-tree only and await the user's explicit commit/push._

## What was built

### Template contract (shared) — `packages/shared/src/index.ts`
- Added `INVITATION_TEMPLATE_IDS = ["classic","modern","romantic","minimal"]`,
  `DEFAULT_INVITATION_TEMPLATE_ID = "classic"`, and
  `resolveInvitationTemplateId(value)` (unknown/missing → default, never throws).
- The chosen id lives in the **design doc** (`design.template`), not a new
  column — design is schemaless `jsonb`, so **no DB migration** and pre-template
  docs render via the default. `design.template` is the only schema change.
- Made `invitationDesignFieldsSchema` **per-field lenient**: each key is now
  `.optional().catch(undefined)` instead of the object being `.partial()`. This
  was a real-bug fix surfaced by the review (see below) — one malformed stored
  value used to fail the whole `safeParse`. Also widened `dateTime` to
  `.datetime({ offset: true })` so `+09:00`-style instants parse.
- Tests: `packages/shared/src/template.test.ts` (added to the package `test`
  script alongside slug.test.ts). 10 tests pass.

### Template infrastructure (web) — `apps/web/src/templates/`
- `types.ts` (TemplateProps: invitation/fields/heroKey), `fonts.ts` (Playfair
  Display + Noto Serif KR + Noto Sans KR via `next/font`, applied per-template
  as CSS-var classNames), `format.ts` (KST-pinned date helpers + Naver
  `mapSearchUrl`), `meta.ts` (editor-facing picker metadata), `index.ts`
  (id→component registry via `templateComponent(value)`).
- Four templates, each owning its whole page (`<main>` + own SCSS module):
  `classic/`, `modern/`, `romantic/`, `minimal/`. Built to match the 4 provided
  design mocks; modern/romantic/minimal authored by a parallel subagent
  workflow, classic by hand as the reference.
- Public renderer `(invitation)/invitations/[slug]/page.tsx` now dispatches:
  `const Template = templateComponent(fields.template)`. Old inline markup
  removed; `page.module.scss` slimmed to just the 404 styles.
- Mock thumbnails copied to `apps/web/public/templates/{classic,modern,romantic,minimal}.png`.

### Pickers (editor + create) — `apps/web/src/app/(site)/dashboard/`
- `template-picker.tsx` — radio-card grid (thumbnail + Korean name), reused by
  both the dashboard create form (posts `design:{template}`) and the editor
  (added `template` to `FIELD_KEYS`; `buildDesign()` always writes it; load
  resolves it via `resolveInvitationTemplateId`). Picker styles in
  `dashboard.module.scss`.

## Verification (all local)
- `pnpm --filter @yours-truly/shared test` → 10 pass; `pnpm --filter @yours-truly/api test` → 20 pass.
- `pnpm --filter @yours-truly/web typecheck` clean; `… lint` clean; `… build` succeeds.
- Visual: ran the dev stack, created 8 temp invitations (4 templates × photo/no-photo)
  + drove headless Chrome to PDF, reviewed each against its mock. An adversarial
  subagent workflow (4 fidelity reviewers + 1 code skeptic) found issues; fixed:
  - **HIGH/MEDIUM (data-loss)**: all-or-nothing design parse → per-field
    `.catch` (above). Was destroying every design field on editor-save (and
    blanking the public page) when any one stored value was malformed.
  - classic: hero 4:5→5:8, sans caption layer (also fixes Playfair digits
    dipping next to hangul), second hairline under the date.
  - modern: hero display lockup enlarged (`clamp(48px,13vw,62px)`) to match the
    mock's cinematic scale.
  - romantic: polaroid photo 4:5→1:1 (square, per mock), sans label layer.
  - minimal: venue card left-aligned + underlined CTA, accent darkened to bronze
    `#806339` (AA contrast at 11px caps).
  - all: `formatWeekdayTime` no longer renders `(토요일)` parens; added
    `100vh` fallback before every `100dvh`; added `overflow-wrap: anywhere`
    alongside every `word-break: keep-all` (long unspaced names/URLs).

### Editor timezone fix — `(site)/dashboard/invitations/[id]/page.tsx`
- Rewrote `isoToLocalInput`/`localInputToIso` to convert against a fixed
  **+09:00** (`KST_OFFSET_MS`) instead of the browser's timezone. Previously the
  ceremony time was round-tripped through the editor's local TZ while the guest
  page renders pinned to Asia/Seoul, so editing from abroad shifted the displayed
  hour (e.g. 13:00 typed in New York → guest page showed 새벽 2시). Now fully
  browser-TZ-independent; verified the exact logic under TZ=UTC / America/New_York
  / Asia/Seoul / Pacific/Kiritimati — all produce display `13:00` ⇄ store
  `2026-10-24T04:00:00.000Z` (matching the seed's documented 13:00 KST).
- Labeled the field `예식 일시 (한국 시간)` so the couple knows the time is Korean local.
- Cleaned up: deleted all `tpl-*` temp invitations; restored `demo-wedding`
  design to its seed shape (removed the template/heroImageKey I'd set for shots).

## Deliberately NOT changed (out of scope / flagged to user)
- classic title ampersand kept gold (consistent gold-`&` signature across all
  four templates; mock shows dark). Low; design call.
- Per-template font code-splitting: all 3 families currently ship on every
  invitation page (registry imports all templates statically). `fonts.ts`
  comment corrected to say so; optimization deferred.

## Switched main to a PR-based flow (later in session)
- Context: the design-templates push went **directly to `main`** and bypassed
  the required `ci` check (admin bypass). User asked to move to PR-based.
- Finding: the deploy was already CI-gated — `yt-deploy.sh` polls `origin/main`
  every 5 min and refuses to ship a SHA until its GitHub Actions checks are
  green. So the gap was purely **branch protection**, not the deploy script.
- Branch protection on `main` (via `gh api PUT .../branches/main/protection`):
  added `required_pull_request_reviews` with **0** required approvals (solo repo
  — CI is the gate, owner merges own PRs), kept the `ci` required status check,
  kept `enforce_admins: false` (owner's choice — admin emergency override
  retained). Also enabled repo `delete_branch_on_merge`.
- Docs updated to match: `deploy/README.md` (pipeline diagram + PR note +
  rollback-via-PR), root `README.md` Production section, and the rollback
  comment in `deploy/yt-deploy.sh`. Deploy machinery itself unchanged.
- Landed via PR (dogfooding the new flow) rather than another direct push.

## Deploy verification + SSH allowlist (end of session)
- Confirmed the deploy on the box (`yt-prod-2`): `/var/www/yours-truly/.deployed_sha`
  = `da6dea50a2ab7defbca1ef568696e8d6a9254047` (= origin/main HEAD, PR #3 merge).
  Deploy log: `DEPLOY OK at da6dea5 (api 200, web 200)`; yt-api/yt-web/yt-deploy.timer
  all active. Templates feature also independently confirmed live via served assets
  (`/templates/{classic,modern,romantic,minimal}.png` → 200).
- **AWS change**: SSH (:22) to the box timed out from this session — SG
  `sg-0a2913fe4d37c882b` only allowed 1.227.122.254/32 + 49.238.154.187/32, and the
  current egress was 121.133.49.178 (home IP rotated; :443 stayed open). Per the
  user's choice, **added `121.133.49.178/32` permanently** to the port-22 ingress
  (`aws ec2 authorize-security-group-ingress`, us-east-1). The two prior /32s were
  LEFT in place (not confirmed stale). AWS CLI authed as IAM `busan-sashimi`.

## End-of-session state (early — superseded by the full-parity build below)
- (Historical snapshot from when this log was first drafted.) Subsequently the
  templates feature + PR-flow + deploy-verification all landed via PRs #3/#4 and
  deployed; see above.

## Full-parity templates — 12 Salon de Letter designs (large multi-phase build)
User asked to recreate 12 of their own product's live invitation designs
(`salondeletter.com/w/<id>`) at **full feature parity**, replacing the 4 simple
templates. Approved a 7-phase plan; executed phase-by-phase, each landed via PR
with green CI (PR-gated `main`):

- **PR #5 (Phase 1)** — expanded `invitationDesignFieldsSchema` (gallery,
  profiles, parents, contacts, timeline, interview, map, transit, reception,
  accounts, tabs, quote, wreath, relationshipStartDate, guestUpload, RSVP/
  guestbook toggles) — per-field `.optional().catch()`; + shared payload
  contracts for the 3 dynamic features. Static content lives in the `design`
  jsonb (no migration).
- **PR #6 (Phase 2)** — 3 tables (`rsvp_responses`, `guestbook_entries`,
  `guest_uploads`, migration `0003`, applied to RDS via the deploy) + public-
  write rate-limited endpoints + owner moderation; guest media under a `g/`
  S3 prefix. 25 api tests.
- **PR #7 (Phase 3)** — themeable section library: a CSS-var `Theme` system +
  `TemplateShell` + ~20 data-driven sections (Hero w/ 6 variants, Calendar,
  Countdown, Gallery, Timeline, Accounts, RSVP, Guestbook, GuestUpload, …).
- **PR #8 (Phase 5/6)** — the **12 templates** (each a Theme + section
  composition), `INVITATION_TEMPLATE_IDS` swapped, registry/meta/thumbnails,
  old 4 retired (default → ivory-editorial; resolver still falls back).
- **PR #9 (Phase 7)** — fidelity pass: rendered all 12 on a fully-populated
  invitation vs. the source captures (adversarial workflow), fixed hero variants
  + palettes, regenerated 2 thumbnails.
- **PR #10 (Phase 4)** — editor expansion: 12 controlled panels + ImageField +
  ManagementPanel; couples can now input every section + manage RSVP/guestbook/
  uploads. Shared `lib/upload.ts`.

Build orchestrated largely via background Workflow fan-outs (capture/analyze 12
designs, build sections, build templates, fidelity-review, panels — though the
panels workflow hit a session usage limit and was finished by the main loop).

Known follow-ups: (1) ~~bucket policy for `g/*`~~ **DONE 2026-06-13** — added
public `s3:GetObject` on `g/*` to both `yourstruly-assets` and
`yourstruly-assets-dev` (`PublicReadUploads` Resource now `[i/*, g/*]`); verified
a `g/` object returns 200 on both. Guest-uploaded photos now display.
(2) minor fidelity polish — secondary rose/olive accent token for
sage/letter-seal, eucalyptus watercolor-wreath artwork, letter-seal wax seal.

Local note: the local `next build` was intermittently blocked by transient
`gstatic` CJK-font (Noto Serif/Sans KR) downloads — environmental; CI builds
fonts reliably and gates every PR. Local DEV renders fine. Seeded 12 `demo-<id>`
invitations (dev DB + dev-bucket photos) for local preview at
`/invitations/demo-<id>` — left in place; clean up when done.

## Wreath + wax-seal fidelity upgrade (new session, branch `feat/wreath-seal-fidelity`)
Stale-log correction first: the three "minor fidelity polish" follow-ups above
were actually already shipped in **PR #13** (`78d6e94`, "feat(web): fidelity
polish — secondary accent, eucalyptus wreath, wax seal"), which landed ~20:01
KST — *after* this log's prior write, so it was never recorded here. PR #13 (by
Fable 5) added the `accent2` tokens (rose/blush/olive on sage-editorial /
sage-storybook / letter-seal, wired into Timeline + ProfileCards), a stylized
leaf-ring "wreath" hero, and a flat wax seal. So item (1) accent2 is **done**.

This session closed the quality gap on the other two (user asked to "upgrade
wreath + seal"; left accent2 as-is):

- **Eucalyptus wreath** — replaced PR #13's flat ring of 24 identical ellipses
  with a **watercolor eucalyptus sprig-wreath**: 12 leaf sprigs ringing an open
  center over a soft radial foliage wash + a blurred background foliage mass.
  Themeable via `currentColor` (tints with `--accent`) with one secondary
  muted-sage tone (`#7d9a7e`) on two sprigs for silver-green depth. Lives in
  `apps/web/src/templates/sections/Hero.tsx` `EucalyptusWreath()` (now static
  markup, no React keys). Used only by the `eucalyptus-wreath` template.
- **Wax seal** — replaced the flat disc with a **convex glossy wax dome**:
  radial-gradient body (bright top-left hotspot → dark rim), `::before` raised
  embossed rim, `::after` pressed inner ring, soft hand-stamped cast shadow.
  `.waxSeal` in `Hero.module.scss`; theme-derived via `color-mix` on `--accent`.
  Used only by `letter-seal` (passes `seal`); cobalt/terracotta share the
  "letter" hero but don't, so they're unaffected.

Process: generated candidates via a background Workflow (4 wreath SVGs + 2 seal
treatments), rendered each in headless Chrome at real in-app size/theme colors,
compared against baselines, and chose `sprigs` + `convex-emboss`. (One workflow
subagent transiently edited `Hero.module.scss` in place despite being asked to
only return code — reverted via `git restore`, then re-integrated deliberately.)

Verify: `pnpm --filter @yours-truly/web typecheck` + `… lint` both clean.
Rendered the real `demo-eucalyptus-wreath` + `demo-letter-seal` pages on the
running dev stack (both 200, both heroes correct; the real `2026.10.24` seal
date fits on one line). No new console warnings — the dev overlay's "1 Issue" is
a **pre-existing** duplicate-image-key React warning from a gallery section on
the demo seeds (duplicate `i/…​.jpg` keys), unrelated to this change. Skipped
the local prod build (known gstatic CJK-font flakiness); CI gates the PR.

Files changed: `apps/web/src/templates/sections/Hero.tsx`,
`apps/web/src/templates/sections/Hero.module.scss`. End state: changes are
local on `feat/wreath-seal-fidelity`, **not yet committed/pushed** — awaiting
explicit commit/push per the usual cadence. Still-open follow-up unchanged:
clean up the 12 `demo-<id>` seeds when done.

## Rename service name "Salon de Letter" → "Yours Truly"

User reported the displayed service name still read "Salon de Letter" and
should be "Yours Truly". Replaced all 6 user-facing occurrences:
`apps/web/src/app/(site)/page.tsx` (wordmark, hero aria-label, hero card title,
footer copyright), `(site)/layout.tsx` (page `<title>` metadata), and
`(site)/dashboard/layout.tsx` (topbar wordmark).

Follow-up (same session): per user request, also updated the JSDoc design-mood
comments in all 8 template files (`templates/*/...Template.tsx`) — each had one
"Salon de Letter" → "Yours Truly" reference. `grep` now confirms zero
occurrences remain under `apps/`.

Still left untouched: the historical session logs (`2026-06-11.md`,
`2026-06-13.md`) + the `salondeletter.com` URL example in this file — these are
historical records, not the displayed service name.

Committed on branch `rename-service-yours-truly` (`96d2fee`), pushed, opened
PR #15, CI (`ci`) passed, squash-merged to `main` as `f62afa8` and deleted the
branch (remote + auto-checkout back to `main`). End state: merged to `main`,
working tree clean.

Log-update follow-up: this merge-outcome note was committed on branch
`chore/log-rename-merge` (`2a8f7dd`), PR #16, CI passed, squash-merged as
`94155ca`.

### Live verification (read-only)

`curl https://www.yourstruly.it/` (canonical www host) after the 5-min
pull-deploy: HTTP **200**, `<title>Yours Truly</title>`, "Yours Truly" ×10 in
the rendered HTML, "Salon de Letter" ×0. Rename confirmed live. Not checked:
dashboard topbar wordmark (behind Naver auth) — same one-line change merged in
PR #15.
