# 2026-06-14

## QR guest-messages plan — review, doc fixes, Phase 1 unblock

**What:** Audited `docs/qr-guest-messages-plan.md` for completeness against the
actual codebase, fixed factual errors in its reuse references, and locked the
one decision that gated Phase 1.

**Findings (plan was well-grounded; 4 reference errors corrected):**
1. Migrations path wrong — plan said `apps/api/src/db/migrations/`; actual is
   `apps/api/drizzle/` (per `drizzle.config` `out: "./drizzle"`). 0003 is latest.
2. `http.js` → `http.ts` for the owner-helper import path.
3. s3 delete helpers reversed — `deleteAssetPrefix` is at `s3.ts:134`,
   `deleteAssetObject` at `:141` (plan had them swapped). Also noted
   `deleteAssetObject` already wraps `DeleteObjectsCommand`.
4. `sessionUserId` is in `apps/api/src/auth/auth.ts`, **not** `http.ts` (used as
   in `guest-uploads.ts:134`). Split the helper list accordingly.

   Line numbers in the plan drift a few lines but point at the right code;
   bucket-policy claim verified correct (`PublicReadUploads` covers `i/*, g/*`,
   managed via CLI per `docs/s3-image-pipeline.md` + 2026-06-13 log; `m/*` stays
   private by default).

**Decision locked:** Open Q #1 (photo privacy) → **truly-private presigned GET**
(non-public `m/` prefix). Rationale: feature is a private couple inbox; public
`g/` model rejected (weaker privacy). Promoted to §1 Locked decisions; §6 #1
marked RESOLVED. Status changed to "approved — Phase 1 ready to implement."

**Still open (do NOT block Phase 1 — backend only):** §6 #4 Korean label wording
(Phase 2–3 concern). #2 notifications, #3 QR-on-invitation-page, #5
captions/video remain out of scope for v1.

**State:** Plan doc updated and self-consistent. No code written, nothing
committed (not a git repo at the working-dir level; nested project is). Phase 1
(shared schemas + `guest_messages` table + migration `0004` + `guest-messages.ts`
route + tests) is unblocked and ready to implement next.

## QR guest-messages — Phase 1 (backend) implemented

**What:** Built the backend per `docs/qr-guest-messages-plan.md` §3.1–3.4.

Files changed:
- `packages/shared/src/index.ts` — `guestMessagesSettingsSchema` (near guestUpload),
  added `guestMessages` to `invitationDesignFieldsSchema`; `M_ASSET_KEY_REGEX`
  (strict UUID form, `m/` prefix), `GUEST_MESSAGE_MAX_PHOTOS=10`, and the
  presign / create / inbox-list wire schemas + types.
- `apps/api/src/db/schema.ts` — new `guest_messages` table (text[] `photo_keys`,
  nullable `read_at`, FK cascade, invitation_id index). Generated
  `drizzle/0004_powerful_annihilus.sql`.
- `apps/api/src/s3.ts` — `createMessagePresignedUpload` (`m/` prefix, NOT public),
  `createPresignedGetUrl` (GET, default 600s), `deleteAssetObjects` (batch);
  extended `deleteAssetPrefix` to also purge `m/<id>/`. Added `GetObjectCommand`
  import.
- `apps/api/src/routes/guest-messages.ts` (new) — POST presign (30/10min) · POST
  create (10/10min; each photoKey must sit under `m/<id>/`) · GET owner inbox
  (presigned GET per photo, unread count) · PATCH read · DELETE (best-effort S3
  cleanup). Registered in `server.ts`.
- `apps/api/src/routes/guest-features.test.ts` — 3 new tests (gating+presign;
  create message-only/photos-only/both + reject empty/foreign-key/over-cap;
  owner list+unread+presigned-url, mark-read, delete, non-owner 401/404). Added
  `guest_messages` to the TRUNCATE.

**Decision note:** plan said invalid-input → 422, but this codebase's
`invalidInput` returns **400** — tests assert 400 to match existing convention.

**Verification (all green):** `pnpm --filter @yours-truly/shared build` ✓ ·
`pnpm --filter @yours-truly/api typecheck` ✓ · lint = no-op for both packages ·
migration `0004` generated and **applied to the local test DB** (docker
`yours-truly-db-1`, `yours_truly_test`) · full API suite **28/28 pass**
(new tests 11–13).

**Left undone / next:**
- Local **dev** DB (`yours_truly`) and **RDS** are NOT yet migrated — only the
  test DB was. Run `pnpm db:migrate` against dev before Phase 2 local work; RDS
  gets `0004` via the deploy pipeline (Phase 4).
- Nothing committed — awaiting explicit "Commit it".
- Phases 2 (guest `/send` page) and 3 (dashboard QR + toggle + inbox) not started.
  Korean label wording (plan §6 #4) still open; doesn't block Phase 1.

## QR guest-messages — committed, pushed, PR opened

Branched `feat/qr-guest-messages-phase1` off `main`; three commits:
`5fcc9a7` docs(plan) · `5417d3a` feat(api) Phase 1 impl · `49dba0e` docs(log).
Pushed to `origin`; opened **PR #23**
(https://github.com/BusanSashimi/yours-truly/pull/23) → base `main`. CI
(`.github/workflows/ci.yml`) triggered, pending at hand-off. Not merged; dev DB
+ RDS still unmigrated (test DB only).

## QR guest-messages — merged + prod deploy verified

CI went green (`ci` pass, 1m13s). PR #23 squash-merged to `main` (`e0a2195`);
feature branch deleted. The 5-min pull-deploy (`yt-deploy.timer` on yt-prod-2 /
EIP 44.197.95.242) shipped it automatically: journal shows
`change detected: fce1453 -> e0a2195, CI green` (16:12:54) →
`DEPLOY OK at e0a2195 (api 200, web 200)` (16:13:56).

Verified read-only over SSH (`ubuntu@44.197.95.242`):
- `/var/www/yours-truly/.deployed_sha` = `e0a2195…` (full match to main HEAD).
- **Migration 0004 applied to RDS** — proven transitively: `yt-deploy.sh` aborts
  before recording the SHA / restarting if `db:migrate` fails (sh:85-87), so
  reaching `DEPLOY OK` ⟹ migrate succeeded; journal also shows drizzle's migrate
  output for the run. (Direct `psql` check not possible — psql not on the box.)
- API health 200; **new routes live**: `POST /api/invitations/<uuid>/messages/presign`
  returns our `{"error":{"code":"not_found"}}` (our handler, not Fastify's default
  404) → `guest-messages` routes are mounted in prod.

Not yet done:
- `m/` privacy AccessDenied end-to-end check deferred to Phase 4 (no `m/` objects
  exist yet; a nonexistent-key GET 403 is ambiguous). Bucket policy remains
  `[i/*, g/*]` only — `m/` private by construction.
- Local **dev DB** still needs `pnpm db:migrate` before Phase 2.

(This section uncommitted on `main`; will land with the Phase 2 branch to avoid a
docs-only redeploy.)

## QR guest-messages — Phase 2 (guest submit page) implemented

Dev DB migrated first: `DATABASE_URL=…/yours_truly pnpm --filter @yours-truly/api
db:migrate` → `guest_messages` now on the dev DB too (test + dev + RDS all at 0004).

Files (all in `apps/web`):
- `src/lib/api.ts` — `presignGuestMessageUpload` + `createGuestMessage`.
- `src/app/(invitation)/invitations/[slug]/send/SendForm.tsx` (new, client island) —
  name + message + multi-photo picker (≤10, object-URL thumbnails, remove); on
  submit re-encodes each photo (`reencodeToJpeg`) → presign → PUT to `m/` → create
  with collected keys; progress `(n/total)`, error + success (💌) screens.
- `src/app/(invitation)/invitations/[slug]/send/page.tsx` (new, server) — resolves
  published invitation via `by-slug`, `notFound()` if absent, disabled→notice,
  else renders SendForm; `generateMetadata` robots noindex.
- `src/app/(invitation)/invitations/[slug]/send/send.module.scss` (new) —
  self-contained brand palette (bare invitation layout loads no site skin).

Design choices / deviations from the plan:
- `openDate` gate is evaluated **client-side** in SendForm (hydration-safe, like
  GuestUpload) instead of server-side, to avoid ISR staleness near openDate; the
  API re-enforces the window on submit regardless.
- Used the plan's §6 #4 default heading "신랑·신부에게 따뜻한 메시지를 남겨주세요"
  (overridable by `guestMessages.prompt`). Still unconfirmed wording.

Verification: `shared build` ✓ · web `tsc --noEmit` ✓ · `next lint` clean ✓ ·
`next build` ✓ (`ƒ /invitations/[slug]/send` compiled, 21/21 pages). The build
needed network for `next/font/google` (Noto Serif KR) — sandbox blocked it
(ETIMEDOUT), re-run with sandbox disabled; unrelated to Phase 2 (the `/send` page
uses a system font stack).

NOT yet done:
- **Interactive e2e** (real photo submit through a browser → row in
  `guest_messages` + objects under `m/`) — needs the running app + a browser;
  deferred. API contract already covered by Phase 1 tests (28/28).
- Phase 2 **not committed** — awaiting explicit go-ahead (this log + the deploy
  note will ride on the Phase 2 branch).
- Phase 3 (dashboard QR + toggle + inbox) not started.

## QR guest-messages — Phase 3 (dashboard) implemented

Added the dashboard surface (`apps/web`):
- `qrcode.react@^4` dependency.
- `panels/QrCard.tsx` (new) — QRCodeSVG display of `${origin}/invitations/<slug>/send`
  (origin read after mount), URL + copy, PNG download (hidden 1024px QRCodeCanvas)
  and SVG download (serialize the svg node); 4-module quiet zone for print scans.
- `invitations/[id]/page.tsx` — `guestMessages` in RICH_KEYS, `updateGuestMessages`
  helper, "QR 메시지·사진 받기" toggle + prompt + open-date in the Features panel,
  and a published-gated QR panel near "공개 페이지 보기".
- `panels/ManagementPanel.tsx` — "받은 메시지 (비공개)" inbox section: list
  sender/message/photo thumbnails (presigned `url`) + per-message delete.
- `lib/api.ts` — `getGuestMessages` + `deleteGuestMessage`.
- `dashboard.module.scss` — QR card + inbox photo-thumbnail styles.

Scope decision: **unread badge / mark-read UI deferred.** The `readAt` column and
`PATCH …/read` endpoint exist (Phase 1), but a non-functional badge or aggressive
auto-mark-on-open are both worse than waiting; the inbox shows a total count like
the sibling sections. Documented as a fast-follow.

Verification: web `tsc --noEmit` ✓ · `next lint` clean ✓ · `next build` ✓ (editor
route 9.4→16.3 kB with qrcode.react + QR + inbox; 21/21 pages; sandbox disabled for
the Google-font fetch as before).

⚠️ Process note: Phase 3 was edited on top of the **`feat/qr-guest-messages-phase2`**
branch (I did not branch first), so the changes are uncommitted there and stacked
on Phase 2's `api.ts`. PR #24 (Phase 2) is now CLEAN/mergeable. Recommended path:
merge #24 → branch `feat/qr-guest-messages-phase3` off updated main → commit Phase 3
cleanly. NOT yet committed. Interactive e2e (browser submit + inbox render) still
pending.
