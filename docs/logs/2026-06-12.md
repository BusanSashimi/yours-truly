# Claude session operations log — 2026-06-12

_Manually written (local Mac session, continuation of 2026-06-11). Times
approximate, KST. Day theme: making the product human-usable (dev env,
dashboard, Naver login) and the S3 image pipeline — including two production
infrastructure cutovers applied over SSH._

## Operations

### Local dev environment — commit `b235d43` → pushed (CI green)
- Trigger: `pnpm dev` failed (`node: .env: not found`); web-researched current
  practice (compose-for-backing-services hybrid, scripts-to-rule-them-all,
  12-factor parity).
- Built: root `compose.yaml` (postgres:16, healthcheck, init script creating the
  test DB too), `pnpm bootstrap` (idempotent: compose up → generate
  `apps/api/.env` → install → migrate dev+test → seed), `pnpm dev` now starts
  the DB first, `pnpm db:seed` (demo account via Better Auth + published
  `demo-wedding`), README rewritten from boilerplate.
- Finding: `apps/api/.env.example` was never git-tracked (`.env*` ignore) — the
  documented quick-start had been broken for fresh clones all along.
- Later ran `pnpm bootstrap` FOR the user (repo scripts may write .env; my own
  file tools are permission-blocked from env paths) and verified `pnpm dev`.

### Dashboard UI — commit `9a2e81f` → pushed, deployed
- `/login`, `/signup` (Better Auth React client, Korean copy), guarded
  `/dashboard` (list/create/delete) and `/dashboard/invitations/[id]` editor
  (v1 design fields, publish/unpublish, KST datetime handling, design-doc
  merge-save). Typed API client parsing responses through shared schemas.
- `/api` rewrite in next.config: browser is always same-origin (nginx plays
  that role in prod, the rewrite in dev) — no browser CORS, zero-config auth
  client.
- E2E through the web origin with `next start` (cookie via rewrite, CRUD,
  public page). Deploy verified via `/login` marker.

### Naver OAuth wiring — commit `f99c250` → pushed, deployed
- `NaverLoginButton` (signIn.social → /dashboard; graceful degrade when
  provider unconfigured); landing CTA wired (was dead); buttons on
  /login+/signup; account linking `trustedProviders: ["naver"]`.
- **Box op (SSH)**: wrote `NAVER_CLIENT_ID/SECRET` (user-provided in chat) into
  `/etc/yours-truly/api.env`, restarted yt-api, verified live authorize URL.
- Verified with fake creds: authorize URL construction, redirect_uri through
  the :3000 rewrite. Gotcha hit again: stale servers on :3000/:4000 gave one
  false verification round; also `next build` while a turbopack dev server ran
  corrupted `.next` (clean rebuild).

### Naver-broken investigation → canonical-www cutover — commit `ee18c25` → pushed, deployed
- Report: "Naver auth doesn't work locally nor on prod". Ultracode workflow
  (4 agents): live cookie probes, repo host-assumption sweep, Naver-rules
  research, adversarial plan review.
- Root causes: (1) user tested mid-deploy (old build, dead CTA); (2) REAL bug —
  Better Auth cookies are host-only; apex-started flows set the OAuth state
  cookie on `yourstruly.it` while the callback returns on www →
  `error=state_mismatch` (proven with curl cookie jars). (3) local: provider
  simply unconfigured.
- **Box ops (SSH), per runbook `docs/canonical-www-cutover.md`**: froze
  yt-deploy.timer (false-positive in-flight check: pgrep self-match) →
  installed updated yt-deploy.sh (`DOMAIN=www.yourstruly.it` — smoke check
  would hard-fail on the 301 otherwise) → `CORS_ORIGIN=www,apex` + yt-api
  restart → new nginx (apex+IP :443 → 301 www; ACME carve-out preserved) →
  verification → unfroze.
- **Cert renewal found broken & fixed**: dry-run failed — NOT webroot config
  (already correct) but apt certbot 2.9.0 vs 2026 ACME (`dns-persist-01`
  unrecognized; finalize-while-pending → orderNotReady). Migrated to snap
  certbot 5.6.0 (apt removed, snap timer armed), added
  `renew_hook = systemctl reload nginx`, dry-run → "all simulated renewals
  succeeded". Cert would have started failing to renew ~August.
- Repo: nginx conf split, deploy-script DOMAIN, api.env.example CORS,
  REBUILD.md verification (www=200/apex=301), dashboard display prefix → www.
- User confirmed prod Naver login works. Local fixed by user adding
  NAVER_* + `BETTER_AUTH_URL=http://localhost:3000` to apps/api/.env
  (+ localhost callback registered in the Naver console; up to 5 allowed).

### S3 image pipeline — commit `44f33ac` → pushed, deployed
- Ultracode workflow: presigned-PUT research (verified: SDK ≥3.729.0 poisons
  presigned URLs with empty-body CRC32 — still true on our 3.1067.0; fix
  `requestChecksumCalculation: WHEN_REQUIRED`; Content-Type unsigned unless
  `signableHeaders`; ContentLength signs → exact-size binding), next/image +
  KakaoTalk og research (5MB scraper cap, ~100-day og cache, direct-URL
  og:image), adversarial review ("sound-with-changes").
- Review-driven hardening: design docs store object KEYS (renderers refuse
  keys outside `i/<thisInvitationId>/`), client-side canvas re-encode (≤2048px
  JPEG: bounds bytes + strips EXIF/GPS + normalizes HEIC), @fastify/rate-limit
  on POST /api/uploads (30/10min), prefix cleanup on invitation delete,
  nginx `/_next/image` limit_req, separate dev bucket.
- **AWS ops (local CLI, busan-sashimi, acct 905418455758)**: created
  `yourstruly-assets` + `yourstruly-assets-dev` (us-east-1): BPA policy-only
  relaxation, public GetObject on `i/*` only (no ListBucket), PUT-only
  single-origin CORS each. **No IAM changes** — `aws-code-deploy` already has
  AmazonS3FullAccess (over-broad; scoped-role swap flagged as follow-up in
  `docs/s3-image-pipeline.md`).
- **Box ops (SSH)**: appended `S3_BUCKET`/`S3_REGION` to api.env; nginx
  reloaded with imgopt rate-limit zone.
- Verified: 20/20 API tests (incl. checksum regression); local e2e vs dev
  bucket (PUT 200; wrong type 403; wrong size 403; public GET; render;
  og:image; optimizer; delete → prefix empty); prod e2e identical, all green.
  One mishap: `NODE_ENV=development next build` crashes the build worker
  (unsupported) — rebuilt without it.
- Test residue: user row `s3-prod-e2e@example.com` remains in prod
  (invitation + photo deleted).
- User confirmed real upload works on prod.

### Memory + logging
- Project memory established (prod SSH access pattern, canonical host, env-file
  permission workaround, git/verification workflow) and this logging rule.
- These two log files written (2026-06-11, 2026-06-12).

## State at end of session

`main` = `44f33ac` deployed. Live: full product loop (signup/Naver → create →
edit + hero photo → publish → KakaoTalk-previewable share). Open items: Naver
검수, IAM scoped-role swap, RSVP, design templates/editor, Korean webfonts,
CloudFront.
