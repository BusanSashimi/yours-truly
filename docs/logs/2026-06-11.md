# Claude session operations log — 2026-06-11

_Manually written (session ran on the local Mac; the `log_operation.py` hook
only ever ran on the EC2 host). Times approximate, KST. This was the session
that built the invitation product spine on top of the post-incident
infrastructure._

## Context at session start

- Repo state: post-incident rebuild stable. Monorepo (web/api/shared), hand-rolled
  scrypt+session auth, RDS Postgres, CI-gated pull deploy. DB schema: users/sessions
  only. Web: boilerplate-derived landing page (uncommitted "Salon de Letter" WIP).
- Prompt: product outline given (wedding invitation maker/host, user-named slugs at
  `yourstruly.it/invitations/<slug>`) → full project reevaluation requested.

## Operations

### Project reevaluation (analysis only)
- Read architecture doc, all app code, nginx/deploy configs, incident docs.
- Verdict: foundation right (SSR, monorepo, Postgres, deploy pipeline); gaps named:
  no media layer, auth conflicts with social-login CTA, no domain model, elevated
  security/abuse stakes. Suggested order: schema → CRUD → public page → auth → S3.

### Invitations table + slug rules — commit `f4479f1`
- `invitations` table (drizzle migration 0001): uuid pk, owner FK cascade + index,
  unique slug, draft/published enum, opaque jsonb `design`, timestamps.
- Shared: `slugSchema` (3–63, lowercase/digits/hyphens, alnum ends) +
  `RESERVED_SLUGS` (~45 entries); node:test suite wired into CI.
- Gotcha: drizzle-kit's CJS loader can't import the ESM-only shared package —
  enum values inlined in schema.ts with a sync comment.
- Verified against dockerized postgres:16 (Postgres.app on this Mac is PG 11 —
  too old for `gen_random_uuid()`).

### Invitation CRUD endpoints — commit `0ffa6d2`
- `/api/invitations`: POST/GET/GET:id/PATCH/DELETE (owner, cookie auth) +
  public `GET /by-slug/:slug` (published only). Ownership failures 404 (no
  existence leak); slug conflicts via DB constraint 23505 → 409; malformed uuid
  params short-circuit. 10-test suite; responses parsed through shared schemas.

### CSS/styling evaluation (analysis only)
- Found: Latin-only fonts vs Korean product (typography gap), single global style
  scope for two products, correct zero-runtime CSS choice. Recommended route-group
  split + per-template token theming + Korean webfont spike.

### Route groups + SCSS — commit `c1b03ba`
- `(site)` and `(invitation)` root layouts (`lang="en"` / `lang="ko"`); globals
  split into shared `reset.scss` + site-only `site.scss`; all styles → SCSS
  (sass dep). Verified isolation via prerendered HTML: invitation tree loads
  exactly one stylesheet (the reset).
- Commit necessarily included the user's landing-page WIP (files moved).

### Public /invitations/[slug] — commit `51610af` → pushed, deployed
- ISR page (60s revalidate) fetching `by-slug` over loopback (`API_ORIGIN`).
  generateMetadata: OG tags + `robots: noindex` (privacy default). Korean
  guest-facing 404. Default template renders v1 design fields.
- Bugs caught during live verification: route was fully dynamic until an empty
  `generateStaticParams` was added (x-nextjs-cache MISS→HIT proven); ceremony
  time pinned to Asia/Seoul (was server-TZ dependent); OG description newlines.
- Also: earlier false verification round caused by a stale server on :3000
  (EADDRINUSE buried in logs) — testing an old build.
- Push: first 3 commits were already on origin; deploy verified live via the new
  invitation 404 page marker (~poll 10).

### Better Auth migration — commit `30805d2` → pushed, deployed
- Decision: AskUserQuestion (Arctic-on-existing-sessions recommended vs Better
  Auth migration); user chose **Better Auth** ("~zero users = cheapest moment").
- Docs fetched (Fastify mount, drizzle adapter, core schema, Naver provider).
- Implemented: BA core tables in our drizzle schema (usePlural, DB-generated
  uuids), Fastify catch-all (set-cookie via getSetCookie — Headers.forEach
  corrupts multi-cookie), invitation routes via `auth.api.getSession`, Naver
  provider env-gated, old auth files + @fastify/cookie + shared auth contracts
  removed. Env: `BETTER_AUTH_SECRET` replaces `SESSION_SECRET`.
- Migration 0002 hand-written DROP+CREATE (generated ALTERs impossible: hex
  session ids don't cast to uuid; NOT NULL adds fail on rows). drizzle-kit's
  interactive rename prompts driven via pseudo-TTY (`yes $'\r' | script`).
  Verified against a prod-simulation DB seeded with legacy-format rows.
- Test finding: BA 403s cookie-authed POSTs without an Origin header (CSRF) —
  tests now send one. 15/15 tests; live-HTTP e2e of the full loop.
- **Box op (SSH not needed — user did it)**: user updated `/etc/yours-truly/api.env`
  (BETTER_AUTH_SECRET, BETTER_AUTH_URL) before push, per warning that the deploy
  would otherwise crash-loop the API.
- Deploy verified: `/api/auth/get-session` live (200 null), API healthy.

## State at end of day

`main` = `30805d2` deployed. Product spine live: email/password auth + invitation
CRUD + public ISR pages on `yourstruly.it`. No UI beyond the landing page yet
(loop is API-only). Naver provider code-ready but unconfigured.
