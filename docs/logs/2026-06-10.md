# Claude session operations log — 2026-06-10

_Manually written (not hook-recorded). The `.claude/hooks/log_operation.py` logger
runs on the EC2 host; this session ran from the **local Mac**
(`/Users/Brian_1/Desktop/Coding/portfolio/yours-truly`) after the compromised box
powered off mid-command at the end of the 2026-06-09 session, so nothing here was
auto-captured. Times are UTC and approximate (`~`) except where a command returned
an exact timestamp._

## Context at session start

- The 2026-06-09 session ended abruptly: it had concluded the app host
  `i-011a7e6377f9f0d18` was root-compromised (XMRig miner, CVE-2025-55182 RCE in
  Next.js 15.0.3 → `ubuntu` `NOPASSWD:ALL` → root), and issued
  `aws ec2 stop-instances`. That stop powered off the box **while Claude's shell
  was running on it**, which is why this session is on the local Mac.
- Forensic snapshot `snap-0ce2c80ac0e98ae88` of the compromised volume already existed.
- Local AWS identity for this session: IAM user `busan-sashimi` (acct `905418455758`),
  **not** the box's `aws-code-deploy` instance role. This distinction turned out to
  matter a lot (see permission map at the bottom).

---

## Prompt 1 — "check why I cannot access my EC2 instance via ssh after restarting it"

Investigated from the Mac via AWS CLI + direct probes.

- `aws sts get-caller-identity` — confirmed local creds = `user/busan-sashimi`.
- `aws ec2 describe-instances i-011a7e6377f9f0d18` — **running**, launched
  `2026-06-10T02:48:31Z` (the user had *restarted* it after the prior session
  stopped it), EIP `44.197.95.242` still attached, SG `launch-wizard-2`
  (`sg-0b7fa275a453b96d5`) unchanged.
- `aws ec2 describe-addresses` — EIP still associated → IP/DNS not the problem.
- SG ingress — port 22 open to `0.0.0.0/0` → firewall not the problem.
- `describe-instance-status` — system/instance checks **ok**, reachability **passed**.
- `nc -vz 44.197.95.242 22` — TCP **succeeds**.
- SSH banner grab + `ssh -vv` — **no banner; handshake hangs** (had to kill it).
- `curl https://44.197.95.242` — **times out** (site not served either).

**Conclusion:** Not an SSH/IP/SG/key problem. The instance is healthy at the AWS
layer but unresponsive at the application layer — the signature of severe
CPU/RAM exhaustion. On this host that's the cryptominer relaunching on the
restart (reboot persistence), re-reserving ~2.3 GB huge pages and pegging CPU, so
`sshd` accepts the TCP connection but can't fork a session. **Restarting the box
re-armed the malware.**

## Prompt 2 — "you mean … I should not connect to this instance via ssh from here on out?"

- Answered: correct for **this** instance — it's root-compromised and slated for
  destruction; don't log in to use/salvage it (untrustworthy output, secret theft
  risk). Forensics should be done from the snapshot, not the live box. The
  **rebuilt** instance is fine to SSH into once locked down (prefer SSM).

---

## Prompt 3 — "How can I rebuild instance?" → decisions gathered

- Discovered the prior session's `deploy/REBUILD.md` + `bootstrap.sh` were written
  **on the box** and never committed — they only exist in the snapshot. Worked from
  the conversation facts instead.
- `aws ec2 run-instances --dry-run` (with and without `--iam-instance-profile
  Name=aws-code-deploy`) — both **would succeed**: local user can launch *and*
  PassRole. Current profile = `aws-code-deploy`.
- Latest clean Ubuntu 24.04 AMI = `ami-0021ac0c2e69d9c55` (2026-06-04).
- Local repo found at `…/yours-truly/yours-truly` (nested). Read `apps/web/package.json`
  (**next 15.0.3**), `deploy/yt-web.service` + `yt-api.service` (**`User=ubuntu`** —
  the privesc), `yt-deploy.sh`, `nginx-yours-truly.conf` (app on localhost, nginx
  80/443 only), `api.env.example`. No `.github/workflows` → deploy is pull-based
  (`yt-deploy.sh` polls `origin/main`), so pushing doesn't redeploy the old box.
- **AskUserQuestion answers:** access = **SSM Session Manager**; IAM role = **reuse
  `aws-code-deploy`**.

---

## Prompt 4 — "proceed" → local hardening + push

Local repo changes (all reversible, in git):

- `apps/web/package.json` — **next `15.0.3` → `15.0.8`** + `eslint-config-next`
  (patches CVE-2025-55182). `pnpm install --lockfile-only` regenerated
  `pnpm-lock.yaml`; confirmed `next@15.0.8` resolves.
- `deploy/yt-web.service`, `deploy/yt-api.service` — run as no-login **`ytapp`**
  from **`/opt/yours-truly`** on **system Node** (`/usr/bin/node`); added
  `NoNewPrivileges`, `PrivateTmp`, `ProtectHome`, `ProtectSystem=full`; dropped
  the stale `postgresql.service` dependency (DB is RDS).
- `deploy/yt-deploy.sh` — repointed `REPO=/opt/yours-truly`, PATH to system bins.
- New `deploy/bootstrap.sh` — clean-host user-data: apt + Node 22 (NodeSource) +
  corepack pnpm + nginx/certbot, create `ytapp`, clone repo to `/opt/yours-truly`,
  build as `ytapp`, install units/nginx/deploy-script, **minimal sudoers**
  (`ytapp` may only `systemctl restart yt-api yt-web`), download **RDS CA** to
  `/etc/yours-truly/rds-ca.pem`. Does NOT write secrets, get TLS, or start the app.
- New `deploy/REBUILD.md` — launch spec + ordered cutover runbook.

Git reconciliation (security-reviewed):

- Local clone was **2 commits behind `origin/main`** (`59fe31b`, `8a628dd` —
  authored `ubuntu@ip-172-31-84-174`, **Jun 9 09:13–09:24**, ~8h *before* the
  17:55 compromise). Inspected the full diffs + grepped for
  `curl|wget|base64|eval|child_process|/dev/tcp`: legit **RDS TLS work**
  (`verify-full` via `DATABASE_CA` in `drizzle.config.ts`/`db/index.ts`/`env.ts`,
  `closeDb` test fix, `rds-provisioning-policy.json`); only pattern hits were inside
  `docs/logs/2026-06-09.md` documenting normal admin curls. **Deemed safe.**
- `git stash -u` → `git merge --ff-only origin/main` → `git stash pop` (no conflicts).
- Because those commits add `DATABASE_CA`, updated `bootstrap.sh` to install the RDS
  CA bundle and `REBUILD.md` to set `DATABASE_CA=/etc/yours-truly/rds-ca.pem`.
- Committed `3ac62cb` ("security: patch RCE + de-privilege services…") **directly to
  `main`** (deploy-from-main hotfix) and `git push origin main` (`8a628dd..3ac62cb`).
  Push used the user's own GitHub creds, not the leaked PAT.

AWS cutover (from the Mac):

- `~02:4x` **`stop-instances i-011a7e6377f9f0d18`** → `running` → `stopping`
  (active mining/credential exposure halted).
- **`run-instances`** new clean box `ami-0021ac0c2e69d9c55` / `t3.medium` /
  `subnet-025af377476c104d5` / SG **`sg-0a2913fe4d37c882b`** (80/443) /
  profile `aws-code-deploy` / user-data `deploy/bootstrap.sh` / tag `yt-prod-2`
  → **`i-08c6c815be484c2e5`** (priv `172.31.84.107`, pub `54.211.215.48`).
  ⚠️ **Launched without `--key-name`** (no SSH key on the box — relevant later).
- Background poll for SSM `PingStatus=Online` — ran 40×15s, all empty.
  **Initially (wrongly) concluded "SSM not registered."**
- Diagnosis: instance has public IP, subnet auto-assigns, SG egress is open → not a
  networking issue. `get-console-output` showed:
  - **`bootstrap complete — base host + build ready`** + cloud-init finished
    `03:48:08Z`, "Scope: all 4 workspace projects" → **bootstrap + full patched
    build succeeded** (script uses `set -e`).
  - SSM agent error: `unable to acquire credentials … Systems Manager's instance
    management role is not configured` → the `aws-code-deploy` role lacked SSM perms.

## Prompt 5 — "it is attached" (user attached `AmazonSSMManagedInstanceCore`)

- Direct `aws ssm describe-instance-information` → **`AccessDeniedException`**.
  **Realized the earlier poll used `2>/dev/null` and was silently swallowing this
  same AccessDenied** — the empty `ssm=` readings meant "my user can't query SSM,"
  not "instance not registered." Corrected the record with the user.
- `aws ssm send-command` → **AccessDenied** too → `busan-sashimi` cannot *drive*
  on-box steps over SSM at all.
- `aws rds describe-db-instances yt-prod` → **works**: endpoint
  `yt-prod.cmn608iignnq.us-east-1.rds.amazonaws.com:5432`, user `yt`, db
  `yours_truly`, status available.
- `aws rds modify-db-instance` (rotate master pw) → **AccessDenied** — the prior
  session's "I have rds:ModifyDBInstance" was via the *box role*, not this user.
- `associate-address` + `terminate-instances` **--dry-run** → both **would
  succeed** → local user has EC2 lifecycle but not RDS/SSM/IAM.
- **`associate-address`** moved EIP `44.197.95.242` (`eipalloc-075d2ee0cb5622fc9`)
  → `i-08c6c815be484c2e5` (`eipassoc-05ed4df90a102d44e`). Confirmed.
- Handed the user the remaining on-box runbook (Session Manager): rotate RDS pw in
  console → write `/etc/yours-truly/api.env` (RDS URL + new pw + `DATABASE_CA` +
  fresh `SESSION_SECRET`) → `db:migrate` → start `yt-api`/`yt-web` → local health →
  `certbot certonly --standalone` (EIP now points here; `:80` freed) → start nginx.

---

## Key identifiers

| Item | Value |
| --- | --- |
| Compromised host (stopped) | `i-011a7e6377f9f0d18` (terminate after verify) |
| Forensic snapshot | `snap-0ce2c80ac0e98ae88` |
| New clean host | `i-08c6c815be484c2e5` (priv `172.31.84.107`) |
| New AMI | `ami-0021ac0c2e69d9c55` (Ubuntu 24.04, 2026-06-04) |
| Locked-down SG | `sg-0a2913fe4d37c882b` (80/443 only) |
| Old wide-open SG | `sg-0b7fa275a453b96d5` (`launch-wizard-2`, 22/3000/80/443) |
| Subnet | `subnet-025af377476c104d5` (us-east-1a) |
| IAM instance profile | `aws-code-deploy` (now has `AmazonSSMManagedInstanceCore`) |
| Elastic IP | `44.197.95.242` (`eipalloc-075d2ee0cb5622fc9`) → new host |
| RDS | `yt-prod` @ `yt-prod.cmn608iignnq.us-east-1.rds.amazonaws.com:5432`, user `yt`, db `yours_truly` |
| Fix commit | `3ac62cb` on `main` |

## `busan-sashimi` (local user) permission map — learned the hard way

| Can | Cannot |
| --- | --- |
| `ec2:Describe*`, `RunInstances` (+ PassRole `aws-code-deploy`), `StopInstances`, `AssociateAddress`, `TerminateInstances`, `GetConsoleOutput` | `ssm:DescribeInstanceInformation`, `ssm:SendCommand` (no SSM at all), `rds:ModifyDBInstance`, `iam:GetInstanceProfile` / IAM writes |

## Mistakes corrected this session (for the record)

1. SSH-failure cause: properly diagnosed as miner-starved host, not SSH/SG/key.
2. SSM "not registered": was actually AccessDenied swallowed by `2>/dev/null`.
3. Assumed RDS/SSM perms carried over from the box role — they did not.
4. Launched the new box without `--key-name`, so SSH fallback isn't available
   without a relaunch; SSM (now fixed) is the access path.

## Cutover completed (~05:1x UTC)

After the user seeded the SSH key (console Session Manager) and rotated the RDS
master password, Claude finished over `ssh ubuntu@44.197.95.242`:

- Wrote `/etc/yours-truly/api.env` (root:ytapp 640) with rotated `DATABASE_URL`,
  `DATABASE_CA`, fresh `SESSION_SECRET`. (First attempt sourced it as `ubuntu` and
  hit Permission denied — re-ran the env steps as `ytapp`, who can read the file.)
- `pnpm --filter @yours-truly/api db:migrate` → migrations applied over verify-full TLS to RDS.
- Started `yt-api`/`yt-web` → local `api 200`, `web 200`, both `active`.
- `certbot certonly --standalone` issued the cert (expires 2026-09-08). nginx then
  failed `-t` because the config `include`s `options-ssl-nginx.conf` + `ssl-dhparams.pem`
  (created by the `--nginx` installer, not `certonly`); copied both from the certbot
  packages (`/usr/lib/python3/dist-packages/...`) → `nginx -t` ok → started.
- External verify from the Mac: `https://yourstruly.it/` 200, `/api/health` 200,
  `www` 200, `http→https` 301, valid Let's Encrypt cert.
- Confirmed snapshot `snap-0ce2c80ac0e98ae88` `completed` 100%, then
  **terminated `i-011a7e6377f9f0d18`**. Final inventory: only `yt-prod-2`
  (`i-08c6c815be484c2e5`) running on the EIP.

## Outstanding (user)

- [x] Rotate RDS master password — done.
- [x] On-box finish (api.env, migrate, services, certbot, nginx) — done by Claude over SSH.
- [x] Verify site + terminate old box — done.
- [ ] **Revoke the leaked GitHub PAT** (repo is public; nothing depends on it).
- [ ] Review CloudTrail for the compromise window; consider trimming `aws-code-deploy` to least privilege.
- Port-22 rule (`1.227.122.254/32`) removed post-cutover → access is console Session Manager (re-add the rule for terminal SSH anytime).

> Note: `docs/security-incident-2026-06-09.md` was authored on the old box and never
> committed — it now exists only in snapshot `snap-0ce2c80ac0e98ae88`. Recreate from
> the snapshot if a committed copy is wanted.

---

# Post-cutover audit + fixes (~05:30–06:00 UTC, session on the new box)

_This session ran **on `i-08c6c815be484c2e5` itself** (working dir `/home/ubuntu`,
so the repo's logging hook did not fire — entry written manually)._

## Audit of the rebuilt host against the incident remediation spec

All core controls verified in place: Next.js **15.0.8** running at fix commit
`3ac62cb`; `yt-api`/`yt-web` as no-login `ytapp` with full systemd sandboxing;
`ytapp` sudo limited to restarting its own units; SG `yt-web-sg-v2` exposing only
80/443 (confirmed by outside-in probe via the EIP); IMDSv2 enforced; secrets
`root:ytapp 640` with RDS CA + verify-full; git remote PAT-free; sshd
password-auth off, only the operator key present (fingerprint matched against
`cutover-manual-steps.md`); full IOC sweep clean (0 huge pages, no drop paths,
`chattr` intact, no crontabs, no pool connections); `unattended-upgrades` active.

Findings: **(1)** certbot renewal still `authenticator = standalone` → would fail
against nginx on :80 at first renewal (~Aug); **(2)** app ports bound `0.0.0.0`
(SG was the only layer); **(3)** `yt-deploy.timer`/`.service` not installed —
deploys are manual; **(4)** 3 inert wrapped-paste fragment lines in
`authorized_keys`; plus the known open items (PAT revocation, CloudTrail review,
role trimming).

## Fix 1 — certbot renewal (standalone → webroot)

- Verified the vhost's ACME location (`root /var/www/html`, redirect-exempt)
  serves end-to-end with a probe file (local + external 200).
- `certbot reconfigure --cert-name yourstruly.it -a webroot --webroot-path
  /var/www/html` → validated via simulated renewal, config saved.
- Added `/etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh` (`systemctl
  reload nginx`) so renewed certs are actually served.
- `certbot renew --dry-run` → **"all simulated renewals succeeded, no renewal
  failures."** Renewal no longer needs port 80 or nginx downtime.

## Fix 2 — bind app ports to localhost (defense-in-depth vs SG-only)

- `apps/api/src/env.ts` — new `HOST` env var, **default `127.0.0.1`**;
  `apps/api/src/server.ts` — `app.listen({ host: env.HOST })` (was `0.0.0.0`).
- `deploy/yt-web.service` — `next start -p 3000 -H 127.0.0.1`; installed to
  `/etc/systemd/system/` + `daemon-reload`.
- `/etc/yours-truly/api.env` — explicit `HOST=127.0.0.1`.
- Edits applied identically to the dev clone (`/home/ubuntu/yours-truly`) and the
  deploy clone (`/opt/yours-truly`); API rebuilt as `ytapp`; both services
  restarted.
- Verified: `ss` shows `127.0.0.1:3000` / `127.0.0.1:4000` only; local health
  200/ok; external `https://yourstruly.it` + `/api/health` 200 through nginx.

**Not yet committed:** `apps/api/src/env.ts`, `apps/api/src/server.ts`,
`deploy/yt-web.service` (+ this log) are working-tree changes in both clones —
commit/push from the dev clone when ready. (`apps/api/.env.example` not updated —
blocked by `.env*` permission rules; `HOST` is documented in `env.ts` instead.)

## CI/CD pipeline build-out (~06:15 UTC)

Audit found no CI and a disconnected CD (yt-deploy.sh installed but no timer —
all deploys manual). Built the full pipeline, keeping the post-incident posture:
no GitHub credentials/runner on the box, deploys stay pull-based.

- **CI:** `.github/workflows/ci.yml` — on PR + push to main: pnpm install →
  shared build → web build (generates gitignored `next-env.d.ts`, so it must
  precede typecheck) → typecheck → lint → API tests against a `postgres:16`
  service container (migrate first; creds match the hardcoded test
  `DATABASE_URL`) → api build.
- **CI gate in deploy:** `deploy/yt-deploy.sh` now queries the public checks
  API (`/commits/<sha>/check-runs`, unauthenticated — repo is public) and only
  deploys a SHA whose checks all completed successfully. 0 checks / pending →
  wait (exit 0); failed → refuse loudly (exit 1, visible in unit status).
  Only called when origin/main moved, so rate limits are a non-issue.
  jq filters tested against live API + synthetic mixed-status payload.
- **CD trigger:** new `deploy/yt-deploy.service` (oneshot, `User=ytapp`,
  `HOME=/opt/yours-truly` for the pnpm store; no `NoNewPrivileges` since the
  script uses the narrow sudoers rule) + `deploy/yt-deploy.timer`
  (OnBootSec=2min, OnUnitActiveSec=5min).
- **Rollback story corrected** (script header + README): with the timer armed,
  a local `git reset` in the deploy clone gets undone next tick — roll back via
  `git revert && git push` through the pipeline, or stop `yt-deploy.timer` to
  freeze deploys.
- **deploy/README.md rewritten** — old-box paths (`/home/ubuntu/deploy-src`,
  `ubuntu` sudoers) replaced with reality (`/opt/yours-truly`, `ytapp`);
  pipeline diagram, install steps, rollback documented.

Planned next (this session): push → install units + script → enable timer →
branch protection on main requiring `ci` → verify first end-to-end deploy.
